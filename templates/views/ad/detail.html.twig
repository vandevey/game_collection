{% extends 'views/base.html.twig' %}
{% import "component/block.html.twig" as block %}

{% block title %} Page nom {% endblock %}


{% block body %}
<section class="Detail">
    <pre>
        {{ dump(itemAd) }}
    </pre>
    <h1>{{ itemAd.title }}</h1>  
    <h2>Offre / Request</h2>  
    <p>{{ itemAd.description }}</p>
    <p>{{ itemAd.author.pseudo }}</p>
    <p>{{ itemAd.author.email }}</p>
    {% if itemAd.getOffer() %}
        <p>{{ itemAd.getOffer().price }} €</p>
        <p>{{ itemAd.getOffer().item }}</p>
    {% endif %}
    <img src="{{ asset('item/' ~ itemAd.item.images[0].path, 'image') }}" title="img">
    
</section>             

{% endblock %} 